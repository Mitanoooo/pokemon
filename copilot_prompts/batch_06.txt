You have access to an agentic browser. Visit the 5 URLs below one by one, analyse each page's HTML structure, and write a JSON config file for each into a local directory called `site_configs/`. Create the directory if it doesn't exist.

For each site:
1. Open the URL in the browser
2. Look at the rendered page — find the product listing area
3. Inspect the HTML to find CSS selectors for: the element wrapping each product card, the product name, the price, stock status, the product link, and pagination
4. Write the result to site_configs/<domain>.json (domain without www., e.g. konsolinet.fi.json)

Output schema — write exactly this to each file:
{
  "site_name": "Human readable shop name",
  "source_url": "the URL you visited",
  "method": "css",
  "selectors": {
    "product_container": "CSS selector wrapping each single product card",
    "product_name": "CSS selector for product title, relative to container",
    "price": "CSS selector for price, relative to container",
    "in_stock": "CSS selector whose presence means in stock (null if not detectable)",
    "product_url": "CSS selector for the <a> link to product page, relative to container"
  },
  "pagination": {
    "type": "none | next_button | url_pattern",
    "selector": "CSS selector for next-page <a> tag, or null",
    "url_pattern": "URL with {page} placeholder e.g. ?page={page}, or null",
    "max_pages": 5
  },
  "confidence": "high | medium | low",
  "notes": "Any caveats: JS rendering, login wall, no products found, Cloudflare block, etc."
}

If a site is blocked or has no visible products, still write the file with confidence: low and explain in notes. Do not skip any site.

URLs for this batch (5 sites):

1. https://www.konsolinet.fi/category/207/pokemon-kortit
2. https://www.spelexperten.fi/fi/seurapelit/pokemon
3. https://pbcards.fi/collections/pokemon
4. https://peliparatiisi.net/en/collections/pokemon-boosterit
5. https://godofcards.com/en-fi/collections/pokemon-cards

Write all 5 JSON files before you stop. This is batch 6 of 8.
